{
  "name": "gemini-schedule-demo",
  "version": "1.0.0",
  "description": "A demo application showing how to use Google's Gemini API to create student schedules",
  "main": "dist/checkpointQuiz-tests.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/checkpointQuiz-tests.js",
    "dev": "ts-node --transpile-only checkpointQuiz-tests.ts",
    "test": "npm run build && node dist/checkpointQuiz-tests.js",
    "manual": "npm run build && node -e \"require('./dist/checkpointQuiz-tests.js').testCreateQuiz()\""
  },
  "keywords": [
    "gemini",
    "ai",
    "schedule",
    "student",
    "demo",
    "typescript"
  ],
  "author": "Student Demo",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
